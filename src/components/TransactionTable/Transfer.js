import React from "react";

function Transfer({
  id,
  causeName,
  targetAmount,
  description,
  receiverAddress,
}) {
  return (
    <div>
      <button>Transfer</button>
    </div>
  );
}

export default Transfer;
