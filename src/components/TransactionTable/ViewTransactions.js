import React from "react";

function ViewTransactions({
  id,
  causeName,
  targetAmount,
  description,
  receiverAddress,
}) {
  return <div>ViewTransactions of Org {id}</div>;
}

export default ViewTransactions;
